# create a class fight with the following attributes

    - _id
    - _type which is COMMERCIAL, SPECIAL or PRIVATE
    - _startingLocation which is a string
    - _ destinationLocation which is a string
    - _fare which is a float

# Create appropriate special member functions 

# Create the following functionalities

    - a function to create 5 objects of flight in a vector
    - a function to delete objects in the vector ( not required if smart pointer are used)
    - a function to find the average of fare of all flights
    - a function to find the first N instance of flights from a container
    - a function to find the _id of the flight with the minimum _fare

# Demostrate every functionalities in Main