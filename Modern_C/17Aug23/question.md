# 1. Create a class Car with the following attributes:

    - _id which is a string value
    - _model which is a string value
    - _type which is either SEDAN, SUV, HATCHBACK   
    - _price which is a float value
    - _fuelCapacity which is in liters and is an integer value
    [ assuming petrol or disesl vehicle only ]

create a following functionalities:

    - a function to create 5 objects on heap for Car type.
        [ use hard-codes values maunally entered by the programmer. no input from user].

    - a function to find and return the average price of all vehicles passed in a list to the function .

    - a function to find and display the _model of the car with highest price. (if there are multiple car with the same hight price, disply _model of all units).
        
    - a function to find and return a list of all cars whose _price is above a threshold value . (passed via parameter to the function).

Demostrate all functionalities by creating a suitable main function