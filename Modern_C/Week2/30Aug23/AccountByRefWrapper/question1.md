
# Conclusion of this question :

    If you want use REFERENCE WRAPPER,
    then must NOT use ONE ARGUMENT of that class.

=----------------------------------------------------------

# Create a class Account with the following attributes:
        - id
        - name
        - category which could be PREMIUM or BASIC.

    - disable copy and move constructor/assignment.
    - keep the following parameterized constructor
        - 1 arg contructor with id
        - 2 arg constructor with id and category
        - 3 arg constructor


# Create a class Customer with the following attrributes
        - name
        - age
        - Account which is a reference to an Account(active/alive) 
    
    - disable copy and move construction/assignment

    - keep the following parameterize constructor
        - 1 arg construtor with name
        - 3 arg construtor

# Create A function that accepts
    a. vector of customer object
    b. A function that accepts vector of customer and return void

# Create a function that initializes 3 objects of Customer in a vector

# Call operation functions with the following lambdas
    a. lambda to find the first object from the container
    b. lambda to find the average age of customer and print the answer
    c. lambda to find the first min element based on Age of customer and print the details of that objects.

[Lambda_Function_has_its_own_scope]
    
